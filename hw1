#include <stdio.h>

int main() {
    int sum = 0;
    int grade;
    
    printf("Введите 5 оценок:\n");
    
    for (int i = 0; i < 5; i++) {
        scanf("%d", &grade);
        sum += grade;
    }
    
    float average = (float)sum / 5;
    
    if (average > 4 && average != 4) {
        printf("YES\n");
    } else {
        printf("NO\n");
    }
    
    return 0;
}
